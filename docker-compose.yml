version: '3.8'

services:
  ferry-detection:
    build: .
    volumes:
      # Mount certs - placeholder path based on settings.py logic for non-TEST mode
      # Adjust host path as needed for local testing
      - ./certs:/config
    environment:
      - TEST_LOCAL=False
      - CHATKEY=${CHATKEY}
      # Add other necessary env vars here
